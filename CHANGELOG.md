# 更新日志 Changelog

本项目的版本变更记录。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.1.9] - 2026-08-01

### 变更
- **桌面焦点策略（取代仅全屏隐藏）**：悬浮层只在「焦点处于桌面」时显示并响应；焦点在任何其他应用窗口（含全屏/无边框全屏游戏、普通应用、全屏视频）上时自动隐藏，回到桌面自动恢复。彻底避免悬浮层在应用窗口聚焦时被鼠标触发悬停提示/放大等误操作。
- 设置项由「全屏应用/游戏时自动隐藏」改为「**只在桌面时显示（应用窗口聚焦时自动隐藏）**」（默认开启，设置 → 常规）；关闭后恢复常显。
- 决策逻辑移至 `src/main/overlay.js`（`decideOverlayVisibility`），更新单元测试；移除已不再使用的全屏判定相关代码。
## [0.1.8] - 2026-08-01

### 修复
- **交互门卫**：悬浮层只有在「鼠标位置的最顶层窗口确实是悬浮层自己」时才启用交互（WindowFromPoint + GetAncestor 判定）。即使桌面上有其他应用窗口（含透明/点击穿透区域）悬浮层在它们之上，鼠标事件也不会再漏到悬浮层触发悬停提示/放大等误操作。
## [0.1.7] - 2026-08-01

### 新增
- **全屏应用/游戏自动隐藏悬浮层**（设置 → 常规 →「全屏应用/游戏时自动隐藏」，默认开启）：每 800ms 检测前台窗口，当全屏应用/无边框全屏游戏占满显示器时自动隐藏悬浮层，回到桌面后自动恢复，避免悬浮层遮挡游戏画面或误响应鼠标（如弹出图标名称提示）。用户正在操作悬浮层时有 4 秒宽限期，不会误隐藏；检测依赖 koffi（GetForegroundWindow / GetMonitorInfoA），原生层不可用时自动降级不生效。

### 修复
- 托盘「显示」后立即重新置底（repin），避免窗口短暂浮到最上层。

## [0.1.6] - 2026-08-01

### 整理与优化
- 修复 `src/main/config.js` 与 `src/main/index.js` 中被损坏的中文注释（恢复为准确的语义注释）。
- 移除 AI 质感功能遗留代码（`ConfigStore.aiTexturesDir`）与空目录（`assets/ai-textures`、`src/renderer/textures`）。
- 清理 `dist/` 中 0.1.0–0.1.4 的旧构建残留，仅保留最新版本产物。
- 重写 `PROJECT_STATE.md`（架构 + 已完成工作 + 交接约定）并纳入版本管理；新增 `CHANGELOG.md`；更新 `README.md` 中的版本号与产物说明。

### 打包
- 精简打包策略为**仅 x64 NSIS 安装包**（按需求移除 arm64 与便携 zip），产出 `dist/灵动桌面 Setup 0.1.6.exe`。

### 修复
- 修复分类条窗框宽度退化问题：容器配置缺失 `w` 时窗框会按内容（最长文件名）收缩。渲染层回退到默认宽度（`DEF_W`），主进程 `loadConfig` 加载时自动补 `w`（自愈），并修复现有 `config.json`（9 个容器补回 w=300）。新增对应单元测试。

## [0.1.5] - 2026-08-01

### 修复
- 基于完整当前源码重建发布（rebuild 0.1.5），补齐运行时依赖（koffi/@koromix、scripts 解包）。

## [0.1.4] - 2026-08-01

### 修复
- 修复打包后托盘图标加载与设置菜单入口。

## [0.1.3] - 2026-08-01

### 修复
- 修复 Windows 开机自启（改用 `app.setLoginItemSettings` + `--autostart`，清理旧版注册表启动项）。

## [0.1.2] - 2026-08-01

### 修复
- 修复停靠栏边缘淡出与图标边界问题；补齐打包所需运行时依赖。

## [0.1.1] - 2026-08-01

### 修复
- 修复 Electron 欢迎窗口在开机自启时弹出（清理旧登录启动项）。

## [0.1.0] - 2026-07-31

### 新增
- FlowDesk 灵动桌面首个可运行版本：桌面卡片化展示、分类浏览、智能推荐、停靠栏、外观定制、自动扫描、系统托盘等 v0.1 功能集。
