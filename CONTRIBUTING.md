# 参与贡献

感谢你参与 FlowDesk。为了让桌面覆盖层保持稳定，请在提交修改前阅读以下约定。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Node.js 22+ 执行 `npm install`。
3. 修改代码并补充相应测试。
4. 执行：

```powershell
npm test
npm run smoke
```

5. 如涉及安装器，再执行：

```powershell
npm run dist:x64
```

6. 提交 Pull Request，说明问题根因、修改范围、用户影响和验证方法。

## 代码约定

- 使用 CommonJS，与现有代码保持一致。
- 不在渲染进程直接启用 Node.js 能力。
- 新 IPC 必须经过 preload 白名单暴露。
- 配置修改必须兼容旧配置缺失字段。
- 文件写入优先使用原子保存，避免断电或异常退出损坏配置。
- Windows 原生调用必须捕获失败并提供降级行为。
- 动画必须在结束或超时后清理临时合成状态。
- 不提交 `node_modules/`、`dist/`、用户配置、图标缓存或调试日志。

## Bug 报告

请提供：

- Windows 版本和系统架构
- 显示器数量及缩放比例
- FlowDesk 版本
- 可重复的操作步骤
- 实际结果和期望结果
- 必要的截图或录屏

请勿上传包含私人文件名、路径或个人信息的配置文件；如确需提供，请先脱敏。

## 功能建议

请描述实际使用场景和希望解决的问题。对于 UI 建议，最好同时提供简单线框图、截图标注或参考产品。

## Pull Request

- 一个 PR 尽量只处理一个主题。
- 不要夹带无关格式化或重命名。
- 确保测试通过且工作区不包含构建产物。
- 用户可见变化应同步更新 README 或 CHANGELOG。
